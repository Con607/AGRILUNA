<%= simple_form_for(@pay_roll_item) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :salary %>
    <%= f.input :discounts %>
    <%= f.input :bonuses %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
