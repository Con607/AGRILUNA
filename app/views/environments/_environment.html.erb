	  <tr id="environment_<%= environment.id %>">
        <td><%= environment.name %></td>
        <td><%= environment.description %></td>
        <td><%= link_to 'Show', environment %></td>
        <td><%= link_to 'Destroy', environment, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %></td>
      </tr>