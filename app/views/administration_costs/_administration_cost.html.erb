	  <tr id="administration_cost_<%= administration_cost.id %>">
        <td><%= administration_cost.event_date %></td>
        <td><%= administration_cost.concept %></td>
        <td><%= administration_cost.display %></td>
        <td class="alnright"><%= number_to_currency(administration_cost.unit_price, precision: 2) %></td>
        <td class="alnright"><%= administration_cost.quantity %></td>
        <td class="alnright"><%= number_to_currency(administration_cost.total, precision: 2) %></td>
        <td><%= link_to 'Image 1', administration_cost.image1.url(:big) unless administration_cost.image1_file_name.nil? %></td>
        <td><%= link_to 'Show', administration_cost %></td>
        <td><%= link_to 'Edit', edit_administration_cost_path(administration_cost) %></td>
        <td><%= link_to 'Destroy', administration_cost, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %></td>
      </tr>